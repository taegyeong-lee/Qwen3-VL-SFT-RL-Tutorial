# ── Data Preparation (Step 0~3) ──
openai
python-dotenv
pandas
numpy
matplotlib
mplfinance
Pillow
PyYAML

# ── Training (SFT / DPO / GRPO) ──
torch>=2.6.0  # FSDP2 requires >= 2.6.0, install with: pip install torch --index-url https://download.pytorch.org/whl/cu124
transformers>=4.45.0
trl>=0.29.0
peft>=0.12.0
datasets
accelerate

# ── Inference / DPO Pair Generation ──
# vllm>=0.6.0
sentence-transformers  # BGE-M3 for DPO reasoning similarity

# ── Monitoring ──
tensorboard

# ── Misc ──
tqdm
