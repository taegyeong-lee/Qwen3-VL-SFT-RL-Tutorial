# ── Data Preparation (Step 0~3) ──
openai==2.24.0
python-dotenv==1.2.2
pandas==3.0.1
numpy==2.2.6
matplotlib==3.10.8
mplfinance==0.12.10b0
Pillow==12.0.0
PyYAML==6.0.3

# ── Training (SFT / DPO / GRPO) ──
torch==2.10.0  # install with: pip install torch --index-url https://download.pytorch.org/whl/cu124
transformers==4.57.6
trl==0.29.1
peft==0.18.1
datasets==4.8.3
accelerate==1.13.0

# ── Inference / DPO Pair Generation ──
# vllm==0.17.1
sentence-transformers==5.3.0  # BGE-M3 for DPO reasoning similarity

# ── Monitoring ──
tensorboard==2.20.0

# ── Misc ──
tqdm==4.67.3
gdown==5.2.1  # Google Drive download
